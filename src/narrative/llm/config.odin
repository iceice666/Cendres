// LLM_Config、backend 選擇 — GDD §9.6
// Backends: Ollama (recommended), llama.cpp, cloud API
// API keys / local endpoint config are NEVER committed to version control.
// TODO(Phase 2): LLM_Config struct, settings UI wiring.
package llm
